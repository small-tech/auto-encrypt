const test                           = require('tape')
const NewAccountRequest              = require('../../../lib/acme-requests/NewAccountRequest')
const { symbolOfErrorThrownByAsync } = require('../../../lib/test-helpers')

test('New Account Request', async t => {
  t.pass('(no unit-specific tests)')
  t.end()
})
